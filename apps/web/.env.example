# Cron authentication
# Shared secret used to authorize Vercel Cron invocations.
# Vercel sends it as `authorization: Bearer <CRON_SECRET>` to /api/cron/* routes.
# Generate a strong random value (e.g. `openssl rand -hex 32`).
CRON_SECRET=your_cron_secret
